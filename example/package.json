{
  "name": "example",
  "version": "1.0.2",
  "main": "src/index.js",
  "scripts": {
    "test": "bun test src/",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun",
    "build:run": "bun run dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "WynkJS comprehensive example — all features in one place",
  "dependencies": {
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "^1.3.0",
    "wynkjs": "file:.."
  }
}
